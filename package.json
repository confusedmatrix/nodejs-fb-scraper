{
  "name": "nodejs-facebook-data-scraper",
  "version": "1.0.0",
  "description": "NodeJS Facebook Data Scraper",
  "main": "build/index.js",
  "scripts": {
    "build": "babel src -d build && cp .env build",
    "build:prod": "babel src -d build && cp .env.prod build/.env",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "argparse": "^1.0.7",
    "debug": "^2.2.0",
    "dotenv": "^2.0.0",
    "fb": "^1.1.1",
    "pg": "^6.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-es2015": "^6.14.0"
  }
}
